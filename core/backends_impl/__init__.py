"""Expose the v3 backends package. `core.backends` (contracts) is a module;
this subpackage `core.backends.impl` holds platform implementations loaded
via file location to dodge the lib/core.py shadowing issue."""
